// fallbackView.js — auto-generated view component

export default function fallbackView(props = {}) {
  const container = document.createElement('div');
  container.className = 'view-panel';
  container.innerHTML = `<h2>Fallback</h2><p>TODO: Implement dynamic rendering.</p>`;
  return container;
}
